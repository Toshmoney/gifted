<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/spinandwin.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spin And Win | Gifted Brainz</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body class=" font-poppins">
    <%- include('../partials/aside') %>

    <div class="main-container">
        <div class="every">
            
            <div class="head">
                <h2>Spin And Win</h2>
            </div>
            <div class="all">
                
                <div class="up">
                    <div class="upp">
                        <% if (user.has_spin) { %>
                            <h4 class="choose" style="color:red">You have already spun today, come back 2mao!!</h4>
                        <% }else{ %>
                        <h4 class="choose">you can only spn once. check out our others reward page for more opportunities.</h4>
                        <% } %>
                    </div>
                </div>
                
    
            </div>
            <div class="btn">
                <button class="st" onclick="alert('No more info for now!')">More Information</button>
                <% if (user.has_spin) { %>
                    <button class="nd" onclick="alert('You have already spun for the day, come back 2mao!')">Already Spun</button>
                <% }else{ %>
                <button class="nd" onclick="window.location.href='/spin-wheel'">Spin Now</button>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>