<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Referral List</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto mt-10">
        <h1 class="text-2xl font-semibold mb-5">Referral List</h1>
        <div class="overflow-x-auto bg-white p-5 rounded shadow-sm">
            <% if (referrals.length > 0) { %>
                <table class="min-w-full bg-white border border-gray-200">
                    <thead>
                        <tr>
                            <th scope="col" class="px-6 py-4 bg-gray-50 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
                            <th scope="col" class="px-6 py-4 bg-gray-50 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                            <th scope="col" class="px-6 py-4 bg-gray-50 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Reward</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <% referrals.forEach(referral => { %>
                            <tr>
                                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900"><%= referral.username %></td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500"><%= referral.status %></td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-green-500 font-semibold">N<%= referral.reward %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <p class="text-center text-gray-600 text-sm">You haven't referred anyone yet. Start referring and earn rewards!</p>
            <% } %>
        </div>
    </div>
</body>
</html>
