<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Course</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .edit-course-container {
            width: 100%;
            max-width: 600px;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 5px;
            color: #333;
        }

        input[type="text"],
        input[type="number"],
        textarea {
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        textarea {
            resize: vertical;
        }

        button {
            padding: 10px;
            background-color: #28a745;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <div class="edit-course-container">
        <h1>Edit Course</h1>
        <form action="/admin/courses/edit/<%= course._id %>" method="POST">
            <label for="title">Course Title</label>
            <input type="text" id="title" name="title" value="<%= course.title %>" required>

            <label for="price">Price</label>
            <input type="number" id="price" name="price" value="<%= course.price %>" required>

            <label for="point">Points</label>
            <input type="number" id="point" name="point" value="<%= course.point %>" required>

            <label for="courseDetails">Course Details</label>
            <textarea id="courseDetails" name="courseDetails" rows="5" required><%= course.courseDetails %></textarea>

            <label for="courseLink">Course Links (comma-separated)</label>
            <input type="text" id="courseLink" name="courseLink" value="<%= course.courseLink.join(', ') %>" required>

            <button type="submit">Save Changes</button>
        </form>
    </div>
</body>
</html>
